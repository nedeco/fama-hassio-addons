configuration:
  enable_sensor_station:
    name: Enable Sensor Station
    description: Activates the synchronization of data from the city sensor stations

  smart_home_type:
    name: Smart Home type
    description: Sets the Smart Home type so that the data is sent in the correct format.

  home_assistant_discovery_prefix:
    name: Home Assistant discovery prefix

  mqtt_client_id:
    name: MQTT Client ID
